I will implement a comprehensive reading content pipeline that runs locally using Python and Ollama (Llama 3), as requested. This approach avoids external API reliability issues in China.

### **1. Create Python Pipeline Script (`scripts/fetch_news.py`)**

I will create a robust Python script to handle the end-to-end process:

* **RSS Fetching**: Uses `feedparser` to collect articles from configured sources (Autosport, Airbus, DW, Tagesschau, etc.).

* **Content Extraction**: Uses `trafilatura` to extract clean text from article URLs, removing ads and navigation.

* **Local LLM Integration**: Connects to your local Llama 3 instance via the `openai` Python client (pointing to `http://localhost:11434/v1`).

* **Processing Steps**:

  1. **Simplification**: Rewrites articles to A1, A2, or B1 levels based on your specific grammar/vocabulary constraints. Ensure the final product is in German because some sources might come in other languages.
  2. **Question Generation**: Generates 3-5 comprehension questions per article.
  3. **Vocabulary Extraction**: Identifies difficult words and provides translations.

* **Storage**: Saves the processed content to `src/data/generated/readings.json`, appending new articles to the existing dataset. Run through another workflow to publish it on Supabase database.

### **2. Define Data Structure**

I will update the `readings.json` schema to include the new metadata fields you requested:

```json
{
  "id": "unique-id",
  "title": "Simplified Title",
  "content": "Simplified German text...",
  "level": "A1", // or A2, B1
  "topic": "f1", // or aviation, news, etc.
  "source": {
    "name": "Autosport",
    "url": "https://..."
  },
  "vocabulary": [
    { "word": "Geschwindigkeit", "translation": "Speed", "pos": "noun" }
  ],
  "questions": [...]
}
```

### **3. Update Frontend Integration**

I will update the React application to properly display these new rich articles:

* **`src/lib/generator.ts`**: Update the `GeneratedText` interface to support `level`, `vocabulary`, and `source` fields.

* **`src/pages/Reading.tsx`**:

  * Add a "Level" badge (A1/A2/B1) to the UI.

  * Display the "Original Source" link.

  * Add a "Vocabulary" section to show the extracted words.

### **4. Dependency Management**

I will create a `requirements.txt` file for the Python dependencies:

* `feedparser` (RSS handling)

* `trafilatura` (Text extraction)

* `llama3` (Local LLM client)

* `beautifulsoup4` (HTML parsing helper)

### **Verification**

* I will verify the script can connect to a local Ollama instance (assuming standard port 11434).

* I will verify the generated JSON structure matches what the frontend expects.

